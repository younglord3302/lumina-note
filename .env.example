# Backend Configuration
PORT=5000
JWT_SECRET=your_jwt_secret_here

# Database Configuration (Optional for Dev, Required for Production)
# If not provided, the app uses local filesystem storage (notes.json/users.json)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/notes-app?retryWrites=true&w=majority
